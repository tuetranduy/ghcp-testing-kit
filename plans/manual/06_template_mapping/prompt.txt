# ============================================================
# STEP 6: FORMAT STANDARDIZATION (Template Mapping)
# ============================================================
# This is a standalone prompt — copy & paste into any AI
# ============================================================
# HOW TO USE:
# 1. Send this prompt AFTER reviewing the Test Cases in Step 5
# 2. Customize the TC ID rules if needed
# 3. Copy the output table → paste it into Excel/Jira/TestRail
# ============================================================

---START---

This is the final step. **Standardize the format** of all the Test Cases generated in Step 5 into a standard Markdown table, suitable for copy/paste into Excel or import into Jira/Zephyr/Xray/TestRail.

## The standard table must have the following columns:

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |

## Rules for building the table:

### TC ID
- Consistent format: `[PROJECT]_[MODULE]_TC_[NUMBER]`
- Example: `CRM_CUST_TC_001`, `CRM_CUST_TC_002`...
- Increment continuously, do not skip numbers

[Customize: If you want a different ID rule, replace the format above.
Example: `TC_LOGIN_001`, `TC_REG_001`...]

### Test Steps & Expected Result
- **MUST be numbered** if there are multiple steps
- Use `<br>` or a `-` list to break lines within a Markdown cell
- Example within a cell:
  ```
  1. Open the Login page<br>2. Enter email: test@mail.com<br>3. Enter password: Test@123<br>4. Click the "Login" button
  ```

### Absolute rules:
- ❌ **DO NOT** omit or shorten any Test Case generated in Step 5
- ❌ **DO NOT** arbitrarily merge different Test Cases
- ✅ If the table is too long → Split into **Part 1, Part 2, Part 3...** and ask me before continuing

---

## Output

Output the result as a file (`.md`) so I can:
- Copy the table directly into Excel/Google Sheets
- Import it into Jira/TestRail/Xray

If the total number of Test Cases exceeds 30, split the file into multiple parts and ask me "Continue with Part X?" before generating more.
